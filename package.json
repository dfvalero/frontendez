{
  "name": "frontendez",
  "description": "frontendez root",
  "version": "0.0.1",
  "private": true,
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "start": "lerna run start --stream --parallel",
    "build": "lerna run build --stream"
  },
  "devDependencies": {
    "lerna": "^3.22.1",
    "typescript": "^4.1.3"
  }
}
